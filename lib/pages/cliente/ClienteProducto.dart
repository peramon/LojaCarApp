import 'package:flutter/material.dart';

class ClienteProducto extends StatelessWidget {
  final img;
  ClienteProducto(this.img);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('TD: Realizar la pantalla de los productos'),
      ),
    );
  }
}
