import 'package:flutter/material.dart';

class GerenteActividad extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('TD: Realizar la pantalla del home de las actividades'),
      ),
    );
  }
}
