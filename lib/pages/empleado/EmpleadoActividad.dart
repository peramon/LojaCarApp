import 'package:flutter/material.dart';

class EmpleadoActividad extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('TD: Realizar la pantalla del home de los empeados'),
      ),
    );
  }
}
